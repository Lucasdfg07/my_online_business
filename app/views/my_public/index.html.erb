<%= javascript_pack_tag 'search' %>

<div class="mt-4">
    <div class="knowing_how">
        <div class="container">
            <h4>Conhecendo seu Público.</h4>

            <hr />
            
            <p>
                Agora vamos fazer uma análise mais precisa do seu público alvo, e direcionar a <b>melhor estratégia</b> para você.
            </p>

            <p>
                Aqui você vai aprender o melhor jeito para <b>gerir</b> as redes criadas anteriormente!
            </p>

            <p>
                Quais redes você criou até agora para o <b>seu negócio</b>?!
            </p>

            <hr />
            
            <%= form_with(model: @network) do |f| %>
                <div class="field">
                    <%= f.select(:name,  SocialNetwork.names.keys, {selected: current_user.social_networks.pluck(:name)}, id: 'dropdown', class: 'form-control w-100', multiple: true) %>
                    <small><b>(Caso não tenha nenhuma, deixe em branco)</b></small>
                </div>
                
                <div class="actions mt-2">
                    <%= f.submit 'Prosseguir', class: 'btn btn-primary' %>
                </div>
            <% end %>

            <hr />

            <%= link_to 'Voltar', root_path, class: 'btn btn-warning' %>
        </div>
    </div>
</div>